// This object can be placed anywhere in the room
// and a sound asset can be assigned to its
// "sound_to_loop" variable (see the Variable Definitions
// window).
// This action plays that sound and stores the ID of the
// played sound in the "my_sound" variable.
my_sound = audio_play_sound(sound_to_loop, 0, 1, 1.0, undefined, 1.0);